#!/usr/bin/env python3
from pathlib import Path


def read(path):
    return Path(path).read_text(encoding="utf-8")


def write(path, text):
    Path(path).write_text(text, encoding="utf-8")


def replace_once(path, old, new):
    text = read(path)
    if old not in text:
        raise SystemExit(f"pattern not found in {path}: {old[:140]!r}")
    write(path, text.replace(old, new, 1))


# --- Schedule Center: Tournament Directory must not disappear when schedule payload is empty. ---
path = "app/src/main/java/com/riftlab/app/ui/ScheduleCenterUi.kt"
text = read(path)
for anchor, insertion in [
    ("import com.riftlab.app.data.TournamentGovernanceProvider\n", "import com.riftlab.app.data.TournamentGovernanceProvider\nimport com.riftlab.app.data.TournamentEditionArchiveRecord\nimport com.riftlab.app.data.TournamentEditionArchiveStore\n"),
    ("import com.riftlab.app.data.TournamentResearchProvider\n", "import com.riftlab.app.data.TournamentResearchProvider\nimport com.riftlab.app.data.Worlds2026QualifiedTeams\n"),
]:
    if insertion.strip() not in text:
        if anchor not in text:
            raise SystemExit(f"import anchor missing: {anchor!r}")
        text = text.replace(anchor, insertion, 1)
write(path, text)

replace_once(
    path,
    '''    val center by MatchSessionStore.scheduleCenter.collectAsState()\n    val standingsCenter by StandingsCenterStore.state.collectAsState()\n    val buckets = remember(center.matches, standingsCenter.tournaments) {\n        buildCompetitionBuckets(center.matches, standingsCenter.tournaments)\n    }''',
    '''    val center by MatchSessionStore.scheduleCenter.collectAsState()\n    val standingsCenter by StandingsCenterStore.state.collectAsState()\n    val archiveCenter by TournamentEditionArchiveStore.state.collectAsState()\n    val buckets = remember(center.matches, standingsCenter.tournaments, archiveCenter.editions) {\n        buildCompetitionBuckets(center.matches, standingsCenter.tournaments, archiveCenter.editions)\n    }'''
)

replace_once(
    path,
    '''    val selectedBucket = buckets.firstOrNull { it.key == selectedBucketKey }\n    val selectedStandings = standingsCenter.standings\n        ?.takeIf { it.tournamentId == selectedBucket?.tournamentId }''',
    '''    val selectedBucket = buckets.firstOrNull { it.key == selectedBucketKey }\n    val selectedArchive = archiveCenter.editions.firstOrNull { it.tournamentId == selectedBucket?.tournamentId }\n    val selectedStandings = standingsCenter.standings\n        ?.takeIf { it.tournamentId == selectedBucket?.tournamentId }'''
)

replace_once(
    path,
    '''                            EventCenterTab.TEAMS -> TeamsView(\n                                standings = selectedStandings,\n                                scheduleMatches = leagueWideTeamMatches(selectedBucket, center.matches),\n                                onTeamClick = { team ->''',
    '''                            EventCenterTab.TEAMS -> TeamsView(\n                                standings = selectedStandings,\n                                scheduleMatches = leagueWideTeamMatches(selectedBucket, center.matches),\n                                knownParticipantCodes = (selectedArchive?.participantTeamCodes.orEmpty() + officialParticipantCodesForBucket(selectedBucket)).distinct(),\n                                onTeamClick = { team ->'''
)

replace_once(
    path,
    '''private fun TeamsView(\n    standings: TournamentStandings?,\n    scheduleMatches: List<ScheduledEsportsMatch>,\n    onTeamClick: (EsportsTeamRef) -> Unit\n) {\n    val teams = remember(standings?.tournamentId, standings?.stages, scheduleMatches) {\n        val fromSchedule = scheduleMatches.flatMap { it.teams }''',
    '''private fun TeamsView(\n    standings: TournamentStandings?,\n    scheduleMatches: List<ScheduledEsportsMatch>,\n    knownParticipantCodes: List<String> = emptyList(),\n    onTeamClick: (EsportsTeamRef) -> Unit\n) {\n    val teams = remember(standings?.tournamentId, standings?.stages, scheduleMatches, knownParticipantCodes) {\n        val fromSchedule = scheduleMatches.flatMap { it.teams }'''
)

replace_once(
    path,
    '''        val fromMatches = standings?.stages.orEmpty().flatMap { stage ->\n            stage.sections.flatMap { section -> section.matches.flatMap { it.teams } }\n        }\n        (fromSchedule + fromRankings + fromMatches)''',
    '''        val fromMatches = standings?.stages.orEmpty().flatMap { stage ->\n            stage.sections.flatMap { section -> section.matches.flatMap { it.teams } }\n        }\n        val knownVariants = knownParticipantCodes\n            .map { it.trim().uppercase() }\n            .filter { it.isNotBlank() && it != "TBD" && it != "—" }\n            .distinct()\n            .map { code -> EsportsTeamRef(id = "", code = code, name = code) }\n        (fromSchedule + fromRankings + fromMatches + knownVariants)'''
)

old_bucket = '''private fun buildCompetitionBuckets(\n    matches: List<ScheduledEsportsMatch>,\n    tournaments: List<EsportsTournamentRef>\n): List<ScheduleCompetitionBucket> {\n    val official = tournaments.mapNotNull { tournament ->\n        val tournamentMatches = matches.filter { match ->\n            sameLeague(match, tournament) &&\n                matchStartDate(match)?.let { StandingsCenterStore.containsDate(tournament, it) } == true\n        }\n        if (tournamentMatches.isEmpty()) return@mapNotNull null\n        ScheduleCompetitionBucket(\n            key = tournament.id,\n            title = StandingsCenterStore.displayTournamentName(tournament),\n            matches = tournamentMatches.sortedBy(::matchStartEpochMs),\n            firstEpochMs = tournamentMatches.minOfOrNull(::matchStartEpochMs) ?: Long.MAX_VALUE,\n            tournamentId = tournament.id,\n            tournament = tournament\n        )\n    }\n\n    val used = official.flatMap { it.matches }.map(::scheduleIdentity).toSet()\n    val fallback = buildFallbackBuckets(matches.filterNot { scheduleIdentity(it) in used })\n    val base = (official + fallback)\n        .distinctBy { it.key }\n        .sortedBy { it.firstEpochMs }\n    return addAnnualResearchPlaceholders(base)\n        .distinctBy { it.key }\n        .sortedBy { it.firstEpochMs }\n}'''
new_bucket = '''private fun buildCompetitionBuckets(\n    matches: List<ScheduledEsportsMatch>,\n    tournaments: List<EsportsTournamentRef>,\n    archivedEditions: List<TournamentEditionArchiveRecord> = emptyList()\n): List<ScheduleCompetitionBucket> {\n    // Tournament existence comes from the Tournament Directory / durable archive. A temporarily\n    // empty schedule only means that the match list is still syncing (or has not been published);\n    // it must never delete the event itself from the directory.\n    val official = tournaments.map { tournament ->\n        val tournamentMatches = matches.filter { match ->\n            sameLeague(match, tournament) &&\n                matchStartDate(match)?.let { StandingsCenterStore.containsDate(tournament, it) } == true\n        }\n        ScheduleCompetitionBucket(\n            key = tournament.id,\n            title = StandingsCenterStore.displayTournamentName(tournament),\n            matches = tournamentMatches.sortedBy(::matchStartEpochMs),\n            firstEpochMs = tournamentMatches.minOfOrNull(::matchStartEpochMs) ?: tournamentStartEpochMs(tournament),\n            tournamentId = tournament.id,\n            tournament = tournament,\n            researchOnly = tournamentMatches.isEmpty()\n        )\n    }\n\n    val officialIds = official.mapNotNull { it.tournamentId }.toSet()\n    val archived = archivedEditions\n        .filter { it.tournamentId.isNotBlank() && it.tournamentId !in officialIds }\n        .map { edition ->\n            val tournament = EsportsTournamentRef(\n                id = edition.tournamentId,\n                slug = edition.slug,\n                startDate = edition.startDate,\n                endDate = edition.endDate,\n                leagueId = edition.leagueId,\n                leagueSlug = edition.leagueSlug,\n                leagueName = edition.leagueName\n            )\n            val editionMatches = matches.filter { match ->\n                sameLeague(match, tournament) &&\n                    matchStartDate(match)?.let { StandingsCenterStore.containsDate(tournament, it) } == true\n            }\n            ScheduleCompetitionBucket(\n                key = edition.tournamentId,\n                title = edition.displayName.ifBlank { StandingsCenterStore.displayTournamentName(tournament) },\n                matches = editionMatches.sortedBy(::matchStartEpochMs),\n                firstEpochMs = editionMatches.minOfOrNull(::matchStartEpochMs) ?: tournamentStartEpochMs(tournament),\n                tournamentId = edition.tournamentId,\n                tournament = tournament,\n                researchOnly = editionMatches.isEmpty()\n            )\n        }\n\n    val directory = official + archived\n    val used = directory.flatMap { it.matches }.map(::scheduleIdentity).toSet()\n    val fallback = buildFallbackBuckets(matches.filterNot { scheduleIdentity(it) in used })\n    val base = (directory + fallback)\n        .distinctBy { it.key }\n        .sortedBy { it.firstEpochMs }\n    return addAnnualResearchPlaceholders(base)\n        .distinctBy { it.key }\n        .sortedBy { it.firstEpochMs }\n}\n\nprivate fun tournamentStartEpochMs(tournament: EsportsTournamentRef): Long = runCatching {\n    LocalDate.parse(tournament.startDate.take(10))\n        .atStartOfDay(ZoneId.systemDefault())\n        .toInstant()\n        .toEpochMilli()\n}.getOrElse { Long.MAX_VALUE }\n\nprivate fun officialParticipantCodesForBucket(bucket: ScheduleCompetitionBucket): List<String> {\n    val year = researchEditionYear(bucket)\n    val isWorlds = internationalCompetitionKind(bucket) == InternationalCompetitionMenu.WORLDS\n    return if (year == 2026 && isWorlds) Worlds2026QualifiedTeams.teams.map { it.code } else emptyList()\n}'''
replace_once(path, old_bucket, new_bucket)

replace_once(
    path,
    '''        identity.contains("demacia cup") || identity.contains("德玛西亚杯") || identity.contains("demacia global invitational") -> InternationalCompetitionMenu.DEMACIA_GLOBAL\n        Regex("(^|[^a-z])wscl([^a-z]|$)").containsMatchIn(identity) -> InternationalCompetitionMenu.WSCL''',
    '''        identity.contains("demacia cup") || identity.contains("德玛西亚杯") || identity.contains("德杯国际邀请赛") || identity.contains("demacia global invitational") -> InternationalCompetitionMenu.DEMACIA_GLOBAL\n        Regex("(^|[^a-z])wsc[il]([^a-z]|$)").containsMatchIn(identity) -> InternationalCompetitionMenu.WSCL'''
)

replace_once(
    path,
    '''    val seasonYear = bucket.matches.mapNotNull(::matchStartDate).firstOrNull()?.year\n    if (!bucketLeagueLabel(bucket).equals("LPL", ignoreCase = true)) {\n        EmptyData("${bucketLeagueLabel(bucket)} 年度积分尚未接入；不会显示 LPL 数据作为替代。")\n        return\n    }''',
    '''    val seasonYear = bucket.matches.mapNotNull(::matchStartDate).firstOrNull()?.year\n        ?: bucket.tournament?.startDate?.take(4)?.toIntOrNull()\n    val league = bucketLeagueLabel(bucket).uppercase()\n    if (league != "LPL") {\n        val message = when (league) {\n            "LCK" -> "LCK 的 Worlds 资格由最终阶段 / 季后赛名次直接产生，不使用 LPL 式年度 Championship Points 表。请查看“规则”和资格路径。"\n            "LCS" -> "LCS 的 Worlds 资格按 Split 3 最终阶段名次直接产生；这里不显示一张不存在的 LPL 式年度积分表。"\n            "CBLOL" -> "CBLOL 的 Worlds 资格按 Split 3 最终名次产生；这里不把“不使用 Championship Points”误报成“积分未同步”。"\n            "LCP" -> "LCP 采用混合资格体系：部分席位由季后赛名次直通，另有席位由 Championship Points 直接决定；当前队伍总分只在可信完整数据可重算/官方总表可用时展示。"\n            "LEC" -> "LEC Worlds 席位按最终阶段资格规则结算；官方第三席描述仍需更明确映射时保持待确认，不套用 LPL 年度积分。"\n            else -> "$league 当前资格机制不等同于 LPL Championship Points；请以该赛区官方规则 / 资格路径为准。"\n        }\n        EmptyData(message)\n        return\n    }'''
)

# --- Handbook: robustly recognize the current final split even if Riot Tournament slug omits 'split_3'. ---
path = "app/src/main/java/com/riftlab/app/data/OfficialHandbookGovernance2026.kt"
replace_once(path, "            isSplit3(token) -> regionalSplit3Rules(tournament, token)", "            isRegionalFinalStage(tournament, token) -> regionalSplit3Rules(tournament, token)")
replace_once(path, "        if (!isSplit3(token)) return null", "        if (!isRegionalFinalStage(tournament, token)) return null")
replace_once(
    path,
    '''    private fun isSplit3(token: String): Boolean = token.contains("split_3") ||\n        token.contains("split-3") || token.contains("split 3") || token.contains("third") ||\n        token.contains("第三赛段")''',
    '''    private fun isSplit3(token: String): Boolean = token.contains("split_3") ||\n        token.contains("split-3") || token.contains("split 3") || token.contains("third") ||\n        token.contains("第三赛段")\n\n    private fun isRegionalFinalStage(tournament: EsportsTournamentRef?, token: String): Boolean {\n        if (isSplit3(token)) return true\n        val league = canonicalLeague(tournament, token)\n        if (league.isBlank()) return false\n        val start = tournament?.startDate?.take(10).orEmpty()\n        // Riot's current Tournament Directory does not always carry "Split 3" in the slug/name.\n        // For known 2026 regional leagues, a tournament beginning in the second half of the season\n        // is the handbook's final split context. This only selects an already verified handbook\n        // snapshot; it does not infer any match result, seed or qualification state.\n        return start.startsWith("2026-") && start >= "2026-07-01"\n    }'''
)

# --- Tournament Edition: current active event should hydrate Completed Events automatically. ---
path = "app/src/main/java/com/riftlab/app/data/TournamentEditionArchive.kt"
replace_once(
    path,
    '''    private var syncJob: Job? = null\n    private var hydrateJob: Job? = null\n    @Volatile private var initialized = false''',
    '''    private var syncJob: Job? = null\n    private var hydrateJob: Job? = null\n    @Volatile private var autoHydrateTournamentId = ""\n    @Volatile private var autoHydrateAttemptEpochMs = 0L\n    @Volatile private var initialized = false'''
)
replace_once(
    path,
    '''        _state.value = TournamentEditionArchiveState(\n            editions = ordered,\n            selectedTournamentId = selectedId,\n            selected = detail,\n            followingCurrent = manualSelectedTournamentId.isBlank(),\n            lastRefreshEpochMs = now,\n            statusMessage = when {\n                ordered.isEmpty() -> "Riot Tournament Directory · 当前没有可归档届次"\n                detail == null -> "已归档 ${ordered.size} 个 Tournament Edition"\n                manualSelectedTournamentId.isNotBlank() -> "已归档 ${ordered.size} 个 Tournament Edition · 浏览 ${detail.edition.displayName}"\n                else -> "已归档 ${ordered.size} 个 Tournament Edition · 当前 ${detail.edition.displayName}"\n            }\n        )\n        persist(ordered)\n    }\n\n    private fun buildDetail(''',
    '''        _state.value = TournamentEditionArchiveState(\n            editions = ordered,\n            selectedTournamentId = selectedId,\n            selected = detail,\n            followingCurrent = manualSelectedTournamentId.isBlank(),\n            lastRefreshEpochMs = now,\n            statusMessage = when {\n                ordered.isEmpty() -> "Riot Tournament Directory · 当前没有可归档届次"\n                detail == null -> "已归档 ${ordered.size} 个 Tournament Edition"\n                manualSelectedTournamentId.isNotBlank() -> "已归档 ${ordered.size} 个 Tournament Edition · 浏览 ${detail.edition.displayName}"\n                else -> "已归档 ${ordered.size} 个 Tournament Edition · 当前 ${detail.edition.displayName}"\n            }\n        )\n        persist(ordered)\n        if (manualSelectedTournamentId.isBlank()) selectedRecord?.let(::maybeAutoHydrateCurrentEdition)\n    }\n\n    private fun maybeAutoHydrateCurrentEdition(record: TournamentEditionArchiveRecord) {\n        if (cachedHydratedHistory(record.tournamentId) != null) return\n        val now = System.currentTimeMillis()\n        if (autoHydrateTournamentId == record.tournamentId && now - autoHydrateAttemptEpochMs < 15L * 60L * 1000L) return\n        autoHydrateTournamentId = record.tournamentId\n        autoHydrateAttemptEpochMs = now\n        scope.launch {\n            val history = runCatching { eventHistoryProvider.fetch(record) }.getOrNull()?.takeIf(::hasHistoryData)\n            val standings = runCatching { historicalStandingsClient.fetchTournamentStandings(record.tournamentId) }\n                .getOrNull()?.takeIf(::hasStandingsRows)\n            if (history != null) synchronized(hydratedHistory) { hydratedHistory[record.tournamentId] = history }\n            if (standings != null) synchronized(hydratedStandings) { hydratedStandings[record.tournamentId] = standings }\n            if (history != null || standings != null) {\n                rebuild(MatchSessionStore.schedule.value, MatchSessionStore.targetMatch.value, StandingsCenterStore.state.value)\n            }\n        }\n    }\n\n    private fun buildDetail('''
)
replace_once(
    path,
    '''        val handbookTeams = OfficialHandbookGovernance2026.participantCodesFor(ref, record.displayName)\n        val handbookParticipantSource = OfficialHandbookGovernance2026.participantSourceFor(ref, record.displayName)''',
    '''        val handbookTeams = OfficialHandbookGovernance2026.participantCodesFor(ref, record.displayName)\n        val officialWorldsTeams = if (record.seasonYear == 2026 && record.family.uppercase() == "WORLDS") {\n            Worlds2026QualifiedTeams.teams.map { it.code }\n        } else emptyList()\n        val handbookParticipantSource = OfficialHandbookGovernance2026.participantSourceFor(ref, record.displayName)'''
)
replace_once(
    path,
    '''        val participantTeams = (record.participantTeamCodes + standingsTeams + matchTeams + handbookTeams)\n            .distinct()''',
    '''        val participantTeams = (record.participantTeamCodes + standingsTeams + matchTeams + handbookTeams + officialWorldsTeams)\n            .distinct()'''
)

# --- Qualification: an official Worlds participant confirmation must override a generic PENDING route. ---
path = "app/src/main/java/com/riftlab/app/data/QualificationCenter.kt"
text = read(path)
start = text.index("        val participantRoutes = edition.participantTeamCodes\n", text.index("private fun buildOfficialRegionalMechanismSnapshot"))
end = text.index("        return QualificationTournamentSnapshot(\n", start)
new = '''        val participantRoutes = edition.participantTeamCodes\n            .map { it.trim().uppercase() }\n            .filter { it.isNotBlank() && it != "TBD" && it != "—" }\n            .distinct()\n            .sorted()\n            .map { teamCode ->\n                val officialWorlds = Worlds2026QualifiedTeams.find(teamCode)\n                if (officialWorlds != null) {\n                    TeamQualificationRoute(\n                        tournamentId = edition.tournamentId,\n                        teamId = stableLocalTeamId(teamCode),\n                        teamCode = teamCode,\n                        targetEvent = "2026 全球总决赛",\n                        status = QualificationTeamState.LOCKED,\n                        route = listOf(\n                            QualificationRouteNode(\n                                id = "$teamCode:worlds-official",\n                                label = "2026 全球总决赛资格",\n                                detail = "${officialWorlds.region} · ${officialWorlds.qualificationOrigin}；官方 Worlds 参赛名单已经确认，不再保留为待确认。",\n                                state = QualificationNodeState.CONFIRMED,\n                                source = officialWorlds.source,\n                                evidence = QualificationEvidence.OFFICIAL\n                            )\n                        ),\n                        source = officialWorlds.source,\n                        evidence = QualificationEvidence.OFFICIAL,\n                        updatedThrough = officialWorlds.checkedAt\n                    )\n                } else {\n                    TeamQualificationRoute(\n                        tournamentId = edition.tournamentId,\n                        teamId = stableLocalTeamId(teamCode),\n                        teamCode = teamCode,\n                        targetEvent = "2026 全球总决赛",\n                        status = QualificationTeamState.PENDING,\n                        route = listOf(\n                            QualificationRouteNode(\n                                id = "$teamCode:official-mechanism",\n                                label = "赛区资格机制已核实",\n                                detail = "Riot 官方规则已明确该赛区的 Worlds 资格机制；该队当前是已锁定、仍可争夺还是已淘汰，等待足够的正式赛果/席位数据后再判定。",\n                                state = QualificationNodeState.PENDING,\n                                source = handbookQualification.second,\n                                evidence = QualificationEvidence.OFFICIAL\n                            )\n                        ),\n                        source = handbookQualification.second,\n                        evidence = QualificationEvidence.PENDING\n                    )\n                }\n            }\n'''
write(path, text[:start] + new + text[end:])

text = read(path)
start = text.index("    private fun buildInternationalParticipationSnapshot(\n")
end = text.index("    private fun build2026LplWorldsSnapshot(\n", start)
new_func = '''    private fun buildInternationalParticipationSnapshot(\n        edition: TournamentEditionArchiveRecord\n    ): QualificationTournamentSnapshot {\n        val participantSource = "Tournament Edition participant set · Schedule / Standings / Completed Events"\n        val pendingSource = "等待赛事官方 / Riot / 已核实 Provider"\n        val officialWorldsTeams = if (edition.seasonYear == 2026 && edition.family.uppercase() == "WORLDS") {\n            Worlds2026QualifiedTeams.teams\n        } else emptyList()\n        val teams = (edition.participantTeamCodes + officialWorldsTeams.map { it.code })\n            .map { it.trim().uppercase() }\n            .filter { it.isNotBlank() && it != "TBD" && it != "—" }\n            .distinct()\n            .sorted()\n        val routes = teams.map { teamCode ->\n            val official = officialWorldsTeams.firstOrNull { it.code.equals(teamCode, ignoreCase = true) }\n            TeamQualificationRoute(\n                tournamentId = edition.tournamentId,\n                teamId = stableLocalTeamId(teamCode),\n                teamCode = teamCode,\n                targetEvent = edition.displayName,\n                status = QualificationTeamState.LOCKED,\n                route = buildList {\n                    add(\n                        QualificationRouteNode(\n                            id = "$teamCode:participant-observed",\n                            label = "参赛席位已确认",\n                            detail = if (official != null) {\n                                "${official.region} · 已进入 Riot Worlds 2026 官方确认参赛集合。"\n                            } else {\n                                "该队已出现在此 Tournament Edition 的可信参赛集合中；这只确认参赛事实。"\n                            },\n                            state = QualificationNodeState.CONFIRMED,\n                            source = official?.source ?: participantSource,\n                            evidence = official?.let { QualificationEvidence.OFFICIAL } ?: QualificationEvidence.PROVIDER\n                        )\n                    )\n                    add(\n                        QualificationRouteNode(\n                            id = "$teamCode:origin",\n                            label = "赛区 / Seed / 晋级来源",\n                            detail = if (official != null) {\n                                "Region ${official.region} · ${official.qualificationOrigin}；Seed 仍只在官方明确后写入。"\n                            } else {\n                                "等待可信映射。未拿到官方或可核实 Provider 记录前，不从队名、排名或对阵自行反推。"\n                            },\n                            state = if (official != null) QualificationNodeState.CONFIRMED else QualificationNodeState.PENDING,\n                            source = official?.source ?: pendingSource,\n                            evidence = if (official != null) QualificationEvidence.OFFICIAL else QualificationEvidence.PENDING\n                        )\n                    )\n                },\n                source = official?.source ?: participantSource,\n                evidence = if (official != null) QualificationEvidence.OFFICIAL else QualificationEvidence.PROVIDER,\n                updatedThrough = official?.checkedAt ?: edition.endDate.ifBlank { edition.startDate }\n            )\n        }\n        return QualificationTournamentSnapshot(\n            tournamentId = edition.tournamentId,\n            title = "${edition.displayName} · 参赛资格来源",\n            targetEvent = edition.displayName,\n            routes = routes,\n            sourceSummary = when {\n                officialWorldsTeams.isNotEmpty() -> Worlds2026QualifiedTeams.SOURCE\n                routes.isEmpty() -> pendingSource\n                else -> "$participantSource + $pendingSource"\n            },\n            note = if (routes.isEmpty()) {\n                "该届国际赛已建立资格档案位；参赛集合本身尚未恢复，等待历史赛程 / Standings / Completed Events。"\n            } else {\n                "官方已经确认的 Worlds 参赛事实会直接标记已锁定；Seed 及更细晋级来源仍按字段级证据单独确认。国际赛本身不显示虚构的 Championship Points 缺失表。"\n            },\n            mechanism = QualificationMechanism.PARTICIPANT_ORIGIN,\n            mechanismEvidence = when {\n                officialWorldsTeams.isNotEmpty() -> QualificationEvidence.OFFICIAL\n                routes.isEmpty() -> QualificationEvidence.PENDING\n                else -> QualificationEvidence.PROVIDER\n            },\n            mechanismDetail = "目标赛事按 Qualification Origin / Region / Seed 建档；Championship Points 只在确实采用该规则的赛区显示。"\n        )\n    }\n\n'''
write(path, text[:start] + new_func + text[end:])

# Research identity: public provider uses both WSCI and WSCL spellings in circulation.
path = "app/src/main/java/com/riftlab/app/data/TournamentResearch.kt"
text = read(path)
text = text.replace('Regex("(^|[^a-z])wscl([^a-z]|$)")', 'Regex("(^|[^a-z])wsc[il]([^a-z]|$)")')
write(path, text)

print("dev72 batch2 tournament/qualification repair applied")
