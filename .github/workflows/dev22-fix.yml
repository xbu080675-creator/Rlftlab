name: RiftLab dev22 compile fix

on:
  push:
    paths:
      - 'tools/dev22_fix.py'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: python tools/dev22_fix.py
      - run: rm -f tools/dev22_fix.py .github/workflows/dev22-fix.yml
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "fix: make dev22 timing state compile safe"
          git push
