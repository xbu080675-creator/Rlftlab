dev.87：同时推进两条刚刚在真实比赛日暴露出的关键链路：本地 AI 真正进入可运行阶段；LPL 首发不再等待官网，改为官方社媒抢先、官网后确认。

本地 AI：接入 LiteRT-LM Android 0.17.0，候选模型固定为 Qwen3 0.6B INT4 No-think 的 LiteRT Community .litertlm 包。模型仍然不随 APK 打包，用户明确选择后才下载；下载完成必须匹配固定 SHA-256。VERIFIED 之后新增“运行真机基准”，会真实加载 Engine 并完成一次短推理，同时检查热状态与耗时；只有真实加载/推理通过才进入 READY，总开关仍由用户手动开启。AI 后端只能从已有 TrendFact 中选择场景和高价值信号，不能创造/改写比赛事实。

LocalAiCore 新增规则优先异步入口：规则结果立即返回，AI 在后台补充；每个 gameId 使用单调 revision，新的比赛帧到达后，旧 AI 结果即使晚回来也会被丢弃，避免过期判断覆盖新 HUD。模型不可用、未启用、超时、热状态过高或结果无效时继续 RuleTrendFallback。

首发链路：新增 LplStartingRosterFeed / LplStartingRosterCenter，独立于 5 分钟赛程刷新，以约 60 秒级检查官方首发镜像。证据支持 TEAM_WEIBO / LPL_WEIBO / OFFICIAL_SITE，并区分 TEXT / IMAGE_OCR / CROSS_CONFIRMED。俱乐部或 LPL 官博先发时即可进入 APP，官网只作为后续更高等级交叉确认源，不再作为“能否显示首发”的门槛；无法确认的一侧继续保持未知，绝不从 roster pool 顺序猜首发。

已用 2026-09-11 晚间真实案例作为首条证据：iG 官方微博 23:00 发布 9 月 12 日 iG vs AL 首发，TheShy / Wei / Rookie / JiaQi / Meiko；LPL 官博随后 23:03 跟进，而官网当时尚未发布。只写入已明确核实的 iG 一侧，不根据截图未完整展示的 AL 内容补猜。

版本 1.0.0-dev.87 / versionCode 87。