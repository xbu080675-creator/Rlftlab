dev.59：国内更新架构从 Gitee Release 大文件镜像调整为“GitHub 唯一正式 Release + APP 内 GitHub 更新加速”。GitHub 继续作为唯一主仓库、构建源、版本源和 APK 发布源；`dev-latest` 同时发布带版本号 APK 与 `latest.json`，顺序保持 APK 先上传、manifest 最后切换。客户端检查更新时先直连 GitHub，直连失败才临时启用仅限 RiftLab 官方 GitHub Release 资源的更新加速通道；不创建 VPNService、不修改系统代理、不接管其他 APP 或其他 RiftLab 网络流量，请求完成后立即关闭加速连接。APK 下载支持 HTTP Range 断点续传，可在直连失败后继续从加速通道续传；安装前继续强制校验 HTTPS、SHA-256、包名、versionCode 与固定 DEV 签名证书。Gitee 保留为国内源码镜像，不再承担 APK 主分发，也不再要求 GitHub Actions 跨境上传大文件。