dev.86：补上本地 AI 的 Model Manager 安装链路，把“设备推荐”推进到真正可管理的模型资产阶段，但仍不把未验证权重当成可运行模型。

新增 LocalModelManager：模型只存放在 filesDir/local_ai_models，与普通 cacheDir 完全隔离；下载使用独立 .part 临时文件，完成后必须做 SHA-256 校验，校验失败立即删除临时文件，不允许进入可用状态。模型 URL 和 SHA-256 都必须由模型目录明确提供，任何一个缺失时下载按钮保持锁定，避免把普通 Hugging Face 权重或错误运行时格式当成 Android 可执行包。

设置页的 LOCAL AI 区域新增 MODEL MANAGER 状态：未安装 / 下载中 / SHA 校验中 / 文件已验证 / 基准通过 / 失败，并显示下载进度、删除模型入口和持久化状态。应用启动后只恢复“已验证模型文件存在”的状态，不自动下载、不自动跑模型、不自动启用。

文件通过 SHA 校验仍然只到 VERIFIED，不会直接解锁 AI 总开关。下一阶段必须接真实 Android 推理 runtime、实际加载模型并执行短延迟 benchmark + 热状态检查，只有二者通过后才允许提升为 READY 并安装 TrendInferenceBackend。任何失败继续使用 RuleTrendFallback，赛事事实层不受 AI 影响。

本版本继续坚持：规则层始终可用、AI 只做场景理解/趋势判断/关键点排序、模型是可选增强、缓存清理不会删除模型。版本目标 1.0.0-dev.86 / versionCode 86。
