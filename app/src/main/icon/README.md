# Launcher icon source

`riftlab_launcher.webp.b64` is the dev.67 launcher icon artwork encoded as Base64 so the repository can keep the exact generated binary image through the text-only file writer used for this build path. Gradle decodes it into the generated resource directory before Android resource packaging.
